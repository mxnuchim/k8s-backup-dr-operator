apiVersion: backup.manuchim.dev/v1alpha1
kind: BackupPolicy
metadata:
  name: test-scheduled
  namespace: default
spec:
  schedule: "0 2 * * *" # Every day at 2:00 AM
  target:
    pvcName: test-data
  retention:
    keepLast: 3
