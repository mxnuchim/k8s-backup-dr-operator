apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backup-storage
  namespace: default
spec:
  accessModes:
    - ReadWriteMany # Multiple pods can mount it
  resources:
    requests:
      storage: 10Gi
